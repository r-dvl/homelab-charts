apiVersion: v1
kind: Service
metadata:
  name: {{ include "immich-machine-learning.fullname" . }}
  labels:
    {{- include "immich-machine-learning.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "immich-machine-learning.selectorLabels" . | nindent 4 }}
