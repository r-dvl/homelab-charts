apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "immich-machine-learning.fullname" . }}-test-connection"
  labels:
    {{- include "immich-machine-learning.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "immich-machine-learning.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
